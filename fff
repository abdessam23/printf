
static void	ft_unsint_t(unsigned int n)
{
	if (n > 9)
		ft_unsint_t(n / 10);
	ft_printchar(n % 10 + '0');
}

int	ft_printnbr_int(int n)
{
	if (n < 0)
	{
		write(1, "-", 1);
		ft_unsint_t(-n);
	}
	else
		ft_unsint_t(n);
	return (ft_numlen(n));
}
